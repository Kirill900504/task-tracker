# Десять следующих улучшений

Написано 18–19.09.2026, после того как закрылись четыре этапа из
`improvements.md` и работа над ошибками. Это второй круг: первый разбор
искал поломки, этот — то, чего в трекере нет, а работа этого просит.

**ВСЕ ДЕСЯТЬ СДЕЛАНЫ 19.09.2026.** Кирилл сказал «делай» — и список закрыт
целиком, каждый пункт отдельным коммитом с объяснением. Ниже он оставлен
как есть: пункт описывает, что мешало и почему решено именно так, а не
иначе. Если что-то из этого захочется переделать, начинать надо с причины,
записанной здесь, а не с кода.

Что всплыло по дороге и стоит знать: два пункта оказались не тем, чем
выглядели. Шестой («история сроков») скрывал вторую дыру — о переносе не
узнавал вообще никто, человек планировал неделю под прежнее число. А
десятый («поручить из мессенджера») столкнулся с уже работавшим правилом:
свободный текст коллеги уходит репликой в обсуждение, и фраза «поручи Игорю
смету» молча ложилась туда же. Правило пришлось развести — у руководителя
текст это поручение, у коллеги без входа в трекер это реплика.

Порядок — по пользе на единицу работы, а не по сложности. Каждый пункт
написан так, чтобы за него можно было взяться сразу: где это в коде, что
именно мешает сегодня и чем это кончится.

Отдельно оговорено то, что Кирилл просил продумать заново, — как
руководители направляют задачи и встречи друг другу. Это пункты 1, 2 и 3:
механизм работает, а привычки вокруг него нет.

---

## 1. Задача, поставленная руководителем, не видна владельцу как поручение. СДЕЛАНО

**Сейчас.** Руководитель может поставить задачу другому — права на это
открыла миграция 0031, и `test:workspace` это проверяет. Но у Кирилла в
трекере она выглядит ровно как его собственная: та же карточка, тот же
столбец, ни слова о том, что поручал не он.

**Почему это важно.** Владелец отвечает за пространство и читает
понедельничную сводку по людям. Когда четырнадцать человек начнут ставить
задачи друг другу, половина списка станет чужими поручениями — и первое,
что он сделает, это перестанет доверять цифрам.

**Что сделать.** Пилюля «от Игоря» на карточке там, где `createdBy` не
пуст и не владелец (`TaskCard`, рядом с «принял»). Плюс фильтр «только
мои поручения» — одна кнопка рядом с «Просрочено».

## 2. Встречу можно собрать, но нельзя предложить. СДЕЛАНО

**Сейчас.** Организатор назначает встречу, участники отвечают «буду /
опоздаю / не смогу». Предложить встречу, не назначая её, нельзя — а
руководитель, зовущий двух других, именно предлагает: у него нет права
занимать чужое время.

**Что сделать.** Состояние «предложена»: те же кнопки, но встреча не
попадает в календарь и не шлёт напоминаний, пока её не подтвердит тот, кого
позвали первым, или владелец. Это не новая сущность — колонка `status` уже
есть, нужен четвёртый вариант и правило перехода.

## 3. Нет ответа на «кто мне что поручил». СДЕЛАНО

**Сейчас.** Раздел «Что от вас ждут» показывает задачи, где человек
исполнитель. Кто их поставил — не написано. Пока ставил один Кирилл, это
было очевидно; с четырнадцатью постановщиками перестанет.

**Что сделать.** Имя постановщика строкой в карточке
(`ManagerScreen.card`), и группировка по нему, когда постановщиков больше
одного.

## 4. Просроченное не поднимает голоса. СДЕЛАНО

**Сейчас.** Просроченная задача выделена цветом в трекере и упомянута в
утренней сводке. Ничего больше не происходит: она может висеть неделями, и
единственный, кто об этом узнает, — тот, кто откроет трекер.

**Что сделать.** Эскалация по сроку молчания: три дня просрочки без единого
слова — строка в сводке владельца (уже есть), семь — отдельное сообщение
постановщику, четырнадцать — вопрос человеку прямо в мессенджер с кнопками
«делаю», «не могу», «прошу перенос». Всё это существующие маршруты, не
хватает только расписания.

## 5. Повторяющиеся задачи не умеют «по будням». СДЕЛАНО (кроме «раз в две недели» — ему нужна точка отсчёта, и это решение Кирилла, а не моё)

**Сейчас.** `recur` знает день, неделю, месяц и год. Половина реальных
поручений — «каждый понедельник и четверг» или «по будням».

**Что сделать.** Не новое поле, а список дней недели вместо одного
(`recur_weekday` → массив). Миграция на одну колонку, и `taskLogic`
меняется в одной функции.

## 6. У задачи нет истории сроков. СДЕЛАНО

**Сейчас.** Срок переносят — старый исчезает. Спросить «сколько раз эту
задачу двигали» нельзя, а это первый вопрос к задаче, которая тянется
третий месяц.

**Что сделать.** Перенос пишет строку в обсуждение, как уже пишут приёмка и
возврат (`itemHistory.recordEvent`). Один вызов; правило «история живёт в
обсуждении» уже принято и работает.

## 7. Поиск не ищет по обсуждениям. СДЕЛАНО

**Сейчас.** `localSearch` смотрит названия и описания. Половина того, что
люди помнят, сказана в обсуждении: «мы же договорились про 25-е».

**Что сделать.** Серверный поиск по `item_comments` — отдельным блоком в
окне поиска, потому что реплики нельзя мешать с задачами в одном списке.

## 8. Нет недельного плана. СДЕЛАНО

**Сейчас.** Есть «Сегодня» и два столбца по срочности. Вопрос «что на этой
неделе» задаётся чаще, чем «что сегодня», и ответа на него нет ни на одном
экране.

**Что сделать.** Панель «Неделя»: семь колонок по дням, задача и встреча
одной карточкой. Данные уже есть (`buildToday` умеет считать день —
достаточно позвать его семь раз).

## 9. Никто не видит, сколько на самом деле длится работа. СДЕЛАНО

**Сейчас.** Считается «в срок / не в срок». Сколько прошло от постановки до
«принял» и от «принял» до «сделал» — не считается, хотя обе даты в базе
есть (`personStats` уже берёт первую).

**Что сделать.** Две цифры в понедельничной сводке: сколько человек думает
перед тем, как взяться, и сколько делает. Это тот же расчёт, из той же
выборки, и он отвечает на вопрос «где затык» точнее, чем количество
просроченных.

## 10. Мессенджер не умеет заводить задачу от лица руководителя. СДЕЛАНО

**Сейчас.** Быстрый ввод («поручи Игорю смету») работает только у владельца:
у коллеги нет строки в таблице аккаунтов, и его текст идёт в обсуждение.
Руководителю, чтобы поставить задачу, надо открыть трекер.

**Что сделать.** Разрешить `quickAdd` и коллеге, у которого есть членство:
заводить задачу в пространстве владельца с `created_by` его собственным.
Механизм есть весь, не хватает ветки в `botPipeline.handleText` — и
подтверждения перед созданием, потому что модель не должна назначать людей
молча.

## 11. Видимость шире правила «вижу только своё» — включая владельца. СДЕЛАНО 23.09.2026

**Было.** `matchesView` (`lib/myRole.ts`) в режиме доски «Все» пропускал
любую задачу пространства, даже если у смотрящего в ней нет роли вовсе —
старое правило «Трекер один на всех: чужое видно для чтения». Кирилл
23.09.2026 отменил его прямо, explicit — «это правило касается и меня»:
каждый, включая владельца, должен видеть только то, где он постановщик,
исполнитель, соисполнитель, наблюдатель или приглашённый участник встречи.

**Как сделано — иначе, чем планировалось строкой выше.** Не правка
`matchesView` (она осталась как есть: `view === "all" → true` — и это
теперь ВЕРНО, потому что к ней приходит уже суженный список), а фильтр
ОДИН РАЗ на входе, в `NewTracker.tsx`: `visibleTasks`/`visibleMeetings`
считаются через чистые `isTaskVisible`/`isMeetingVisible`
(`lib/itemVisibility.ts`, с тестами) и раздаются во ВСЕ панели вместо
сырых `tasks`/`meetings` — доску, встречи, календарь, поиск, «Сегодня»,
уведомления браузера (`useNotifications` тоже слал бы тосты и системные
уведомления по ЧУЖИМ просрочкам — тот же класс утечки, что и остальное).
`CalendarPanel`, `SearchOverlay`, `TodayScreen` не пришлось трогать вовсе:
они не читают данные сами, только рисуют то, что им передали, — правило
живёт в одном месте, а не в семи.
Отдельного экрана «Что от вас ждут»/`ManagerScreen` в коде уже не было
(снесён 19.09.2026, см. соответствующую запись); его роль на телефоне
играет вкладка «На приёмке» (`ReviewScreen`/`awaitingReview`) — сужена не
до «видимого мне», а до «где я САМ постановщик» (`myReviewTasks`):
соисполнителю или наблюдателю чужая задача видна на доске, но решать за
постановщика ей не место.

**Ловушка, которую поймал именно тест, а не чтение кода.** Первая версия
`isTaskVisible`/`isMeetingVisible` внутри звала `isMine()` — и `isMine()`
для владельца возвращает `true` БЕЗУСЛОВНО, на любую строку, потому что
отвечает на другой вопрос («может ли он это редактировать» — да, у себя в
пространстве всегда). Значит владелец увидел бы видимым «своим» вообще
всё пространство — ту же дыру, которую всю эту правку и закрывали, только
одной функцией глубже. `itemVisibility.test.ts` уронил ассерт на первом
же прогоне. Чинится не патчем на месте, а отдельной функцией с другим
именем и другой семантикой — `isCreatedByMe()` в `lib/ownership.ts`,
сравнивает `createdBy` буквально, без поблажки владельцу; `isMine` и
`isCreatedByMe` не взаимозаменяемы, и путать их — это же самое, ещё раз.

**Два места нарочно шире, и это решение, а не забытая правка.**
«Загрузка» (кто чем занят) и проверка занятых слотов при назначении
встречи должны знать о ЛЮБОМ человеке, а не только о тех, с кем видящий
уже связан общей работой, — иначе первая станет бесполезной для
делегирования, а вторая не заметит реального конфликта. Оба места отдают
только числа/факт занятости, не содержание чужой задачи или встречи, и
получают его через отдельный явный проп (`allTasks` у `TasksPanel`,
`allMeetings` у `MeetingsPanel`), а не молчаливым обращением к сырым
данным в обход правила.

**Задеты и прошли:** `npx tsc --noEmit`, `npm test` (620/620, включая
новые `itemVisibility.test.ts` и добавленные case'ы в `ownership.test.ts`),
`npm run lint`, `npm run build` — все чистые. `kanban.test.ts` не задет
вовсе (правило столбцов не изменилось, изменилось только то, какие
задачи вообще доезжают до доски).

**⚠ Мысли утекали хуже, чем задачи — СДЕЛАНО (браузерная часть) 23.09.2026.**
`NewTracker.tsx` отдавал в `IdeasPanel`, в `SearchOverlay` и в счётчик
мобильной вкладки весь массив `ideas` из `useTrackerData` без единого
фильтра — а `db.from("ideas").select("*")` в `useTrackerData.ts` читает ВСЁ
пространство. Значит любая чужая мысль любого коллеги была видна в панели
«Идеи и мысли» у каждого, кто открыл трекер, — не только у того, кому её
отправили. `isMine()` для этого не годилась: она отвечает на вопрос «можно
ли править», а не «видно ли», и у владельца возвращает `true` безусловно
(пустой `myUserId` означает «моё» по соглашению `lib/ownership.ts`) — то
есть не решила бы вопрос и для самого Кирилла. Починено: `NewTracker.tsx`
считает `myIdeas` — сравнение `createdBy` напрямую с тем, кто сейчас
смотрит, — и раздаёт этот список во все три места вместо сырого `ideas`.

**Важная поправка к тому, что здесь было написано днём раньше:** утверждение
«в базе нет получателя мысли вовсе» было ошибкой аудита. Получатель есть, и
устроен добротно — таблица `idea_recipients` (миграция 0019, есть с самого
начала многопользовательской части): `idea_id`, `assignee_id`, `seen_at`,
`converted_task_id`. `SendMenu` → `api/telegram/send` при отправке мысли
УЖЕ вставляет туда строку и УЖЕ проставляет `ideas.sent_at` — просто
`useTrackerData` никогда не читал эту таблицу на клиенте, и браузер о ней
не знал вовсе. Значит рабочий канал «получатель нажал В работу в Telegram»
не пострадал от сегодняшней правки — он всё время шёл в обход браузерного
списка, через кнопку под сообщением бота с `recipientId` в её данных.
Что это меняет для п. 12 ниже — см. его новый текст.

## 12. У мысли есть получатель в базе, но нет его на экране — и нет «поделиться». ЧАСТИЧНО СДЕЛАНО

**Сейчас, если точно.** `idea_recipients` уже хранит, кому мысль отправлена,
видел ли получатель её (`seen_at`) и во что она превратилась
(`converted_task_id`). Бот использует это в полную силу: кнопка «В работу»
под пересланным сообщением бьёт именно в конкретную строку получателя.
Чего действительно нет: **браузер эту таблицу не читает**, поэтому в
трекере (не в мессенджере) отправленная мысль никак не выделена для
получателя отдельным блоком, как обещано в `docs/multiuser.md`, п. 5, — а
после сегодняшней правки она там просто не видна вовсе, раз получатель не
её автор. И нет операции **«поделиться»**: сегодняшняя «отправить» ничего
не убирает у автора (в `ideas` нет удаления и нет второго списка,
`sent_at` — просто отметка) и не даёт получателю выбора «получить» /
«получить и сохранить» — просто молча заводит строку в `idea_recipients` и
шлёт сообщение в Telegram, а тому, кто открыл именно трекер, а не чат, об
этом сегодня сказать нечем.

**Что просит Кирилл (23.09.2026).** Две разные операции:
— **«Поделиться»** с одним человеком или группой — мысль остаётся в
  списке автора как была, и дополнительно становится видна получателям;
  ничего не подтверждается, это просто «покажи ему тоже».
— **«Отправить»** одному или нескольким — мысль исчезает из списка
  автора сразу. Получатель видит её и обязан ответить одним из двух:
  «Получить» (прочитано, у себя как мысль не сохраняет) или «Получить и
  сохранить» (мысль появляется в его собственном списке мыслей, с текущей
  датой, как только что записанная, и встаёт в общий порядок по этой
  дате — а не по дате, когда её впервые записал отправитель).

**Что сделать — переоценено вниз, плумбинг уже есть.**
`idea_recipients` почти покрывает «отправить»: не хватает лишь различать
статус («доставлено» vs «получено» vs «получено и сохранено» — сейчас есть
только факт строки и `converted_task_id`), явного удаления/скрытия мысли у
автора при «отправить» (сейчас её не убирает ничто), и экрана
подтверждения в браузере (кнопками, как `Ask.tsx`/`TaskAnswer` — в боте
уже есть похожее). «Поделиться» — отдельная семантика, которой в
`idea_recipients` пока нет вовсе (там предполагается, что получатель
либо ещё не видел, либо превратил в задачу — не «видит наравне с
автором постоянно»); проще всего как булев флаг на строке
`idea_recipients` или отдельная лёгкая таблица `idea_shares`, а не
переиспользовать ту же. Читать `idea_recipients`/`idea_shares` в
`useTrackerData` и подмешивать «доступные мне чужие мысли» к `myIdeas` в
`NewTracker.tsx` рядом с уже написанным фильтром — расширение того же
места, а не второй путь.

---

## Что НЕ рекомендуется, хотя просится

**Личные пространства руководителей.** Своё, отдельное от Кирилла, с
собственными людьми. Это второй трекер внутри первого, и первое же
«а почему я этого не вижу» перечеркнёт всю затею. В `multiuser.md` это уже
решено: пространство одно. (Кто что в нём видит — с 23.09.2026 отдельное
правило, п. 11 выше, и оно уже не «владелец видит всё».)

**Настройки уведомлений на человека.** Четырнадцать выключателей, которых
никто не трогал, — так это названо в `multiuser.md`, и сводка вместо ленты
(миграция 0030) убрала главную причину их хотеть. Сначала должно появиться,
что именно мешает.

**Вложенные задачи.** Их просят всегда и везде. Здесь они превратят плоский
список, который читается за десять секунд, в дерево, которое надо
раскрывать. Если понадобится связь — она уже есть: задача и встреча
ссылаются друг на друга строкой в обсуждении (`itemLink`).

---

## Что нашла сквозная диагностика 20.09.2026

Разбор после того, как закрылись десять пунктов Кирилла по перестройке и
блок ботов. Первые четыре находки исправлены в тот же день и здесь
записаны только затем, чтобы их не завели заново; пятая не сделана
намеренно.

**Из «Завершённых» не было выхода.** Доска стала односторонней — назад
карточку не перетащить, — а кнопки, которой задачу открывают заново, не
существовало. То, что на неё было похоже (галочка «сделано»), снимало
`status`, но принятую задачу держит закрытой ещё и `approval_state`, а эта
колонка серверная. Теперь набор колонок один на все входы —
`lib/reviewWork.REOPEN_PATCH`.

**Кнопка «💬 Ответить» у постановщика не делала ничего.** Стояла под каждой
карточкой, которую бот ему присылает, и отвечала «эта кнопка не для вас».
То есть написать в обсуждение из мессенджера он не мог вовсе: его
свободный текст — поручение, а не реплика, и другой двери нет.

**Хроника и мессенджер подписывались ролью, а не именем.** «Владелец принял
работу» при четырнадцати постановщиках не отвечает на единственный вопрос,
который к строке задают. Реплика руководителя вовсе приходила подписанной
«Коллега».

**Сводка уведомлений уходила без кнопок**, хотя её верхняя группа
называется «Ждут вашей приёмки».

**Руководитель-постановщик не мог принять работу из мессенджера —
СДЕЛАНО в тот же день.** Весь набор «Принять / Вернуть / Продлить /
Напомнить» был привязан к чату владельца (таблица аккаунтов), а чат
руководителя живёт на строке человека: он получал сводку «ждут вашей
приёмки» и шёл открывать трекер. Кирилл, прочитав это, ответил
коротко: «надо чтоб каждый человек мог принять работу из мессенджера и
делать любые манипуляции, так как сейчас мы делаем абсолютно
равноправный для всех постановщик задач».

Чинилось не кнопкой: чат превращается в актора (`lib/botActor`), и
права считаются по `created_by` — тем же способом, каким их считают
маршруты трекера. Вместе с кнопками переехали и незакрытые вопросы
(причина возврата, итог встречи, шаги мастера): их разбирает одна
функция для обоих чатов, а помнит каждый чат у себя. Подробности — в
правиле «ПОСТАНОВЩИК в боте» в CLAUDE.md.
